name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: design-mentor-ai/package-lock.json

      - name: Install dependencies
        working-directory: design-mentor-ai
        run: npm ci

      - name: Build plugin
        working-directory: design-mentor-ai
        run: npm run build

      - name: Prepare deployment
        working-directory: design-mentor-ai
        run: |
          mkdir -p ../deploy
          cp -r dist ../deploy/
          cp index.html manifest.json ../deploy/
          if [ -d "assets" ]; then cp -r assets ../deploy/; fi
          # Copy public folder contents (including images) to deploy root
          if [ -d "public" ]; then
            echo "Copying public folder contents..."
            cp -r public/* ../deploy/
          else
            echo "Warning: No public folder found!"
          fi
          # Verify images were copied
          if [ -d "../deploy/images" ]; then
            echo "Images folder exists in deploy directory"
            ls -la ../deploy/images/ | head -5
          else
            echo "Error: Images folder not found in deploy directory!"
          fi
          if [ ! -f "../deploy/.nojekyll" ]; then touch ../deploy/.nojekyll; fi

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: deploy

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

